# Python export env
.venv*/
__pycache__/
*.pyc

# Large model artifacts — do NOT commit (regenerate with export/export_laya_pte.py)
model-src/hf/
export/*.pte
export/*.pte.data
*.safetensors
*.onnx
*.onnx.data

# RN / Node
node_modules/
.expo/
*.log
int4-gpu/

# Bundled model copies in the RN app (regenerate/copy from export/); tiny.pte stays tracked
rn-demo/assets/models/*.pte
!rn-demo/assets/models/tiny.pte

# macOS
.DS_Store

# Accidental root npm/pnpm stubs (the app lives in rn-demo/)
/package.json
/pnpm-lock.yaml
